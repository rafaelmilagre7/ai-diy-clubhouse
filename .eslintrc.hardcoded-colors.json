{
  "ignorePatterns": [
    "src/components/certificates/**",
    "src/components/learning/certificates/**"
  ],
  "rules": {
    "no-restricted-syntax": [
      "warn",
      {
        "selector": "Literal[value=/#[0-9A-Fa-f]{3,8}/]",
        "message": "❌ Não use cores hexadecimais hardcoded. Use variáveis CSS do design system (hsl(var(--aurora-primary))) ou chartColors de chart-utils.ts. EXCEÇÃO: Componentes de certificado (PDF/PNG export) estão isentos."
      },
      {
        "selector": "TemplateElement[value.raw=/#[0-9A-Fa-f]{3,8}/]",
        "message": "❌ Não use cores hexadecimais hardcoded. Use variáveis CSS do design system (hsl(var(--aurora-primary))) ou chartColors de chart-utils.ts. EXCEÇÃO: Componentes de certificado (PDF/PNG export) estão isentos."
      }
    ]
  }
}
