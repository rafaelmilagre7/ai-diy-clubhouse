// DEPRECATED: Use useCleanOnboarding instead
import { useCleanOnboarding } from './useCleanOnboarding';

export const useOnboarding = () => {
  return useCleanOnboarding();
};

// Todo o código foi movido para useCleanOnboarding para uma implementação mais limpa